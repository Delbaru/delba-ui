// Запасной масштаб, пока генератор не написал `core/scale.ts` (импорт `./scale` берёт файл
// раньше папки): проект на старом скрипте сборки получает умолчания кита, а не ошибку импорта.
export { DEFAULT_SCALE as SCALE } from '../base/scale';
